import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    view: {
      width: '100%',
      marginVertical: 32,
      marginHorizontal: 0,
    
    },

    text:{
     textAlign: 'center',
     color: 'white',
     fontSize: 24,
    },

    subtext:{
        textAlign: 'center',
        color: 'gray',
        fontSize: 16,
       },
  });